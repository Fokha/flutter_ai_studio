name: ai_studio
description: Professional Multi-Model AI Chat Platform
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_markdown: ^0.6.18
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  file_picker: ^6.1.1
  flutter_highlight: ^0.7.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
